apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-training-service
  namespace: lucascarvalho
spec:
  replicas: 1
  selector:
    matchLabels:
      app: model-training
  template:
    metadata:
      labels:
        app: model-training
    spec:
      containers:
      - name: training-service
        image: luccaspc/recommendation-service:1.0
        env:
        - name: MODEL_PATH
          value: "/app/shared/models/fpgrowth_model.pkl"
        - name: CENTRAL_DATASET_PATH
          value: "/data/2023_spotify_ds1.csv"
        - name: USER_DATASET_PATH
          value: "/data/user_dataset/2023_spotify_songs.csv"
        - name: CHECK_INTERVAL
          value: "300"
        volumeMounts:
        - name: model-storage
          mountPath: /app/shared/models
        - name: dataset-volume
          mountPath: /data
          readOnly: true
        - name: user-dataset-volume
          mountPath: /data/user_dataset
          readOnly: true
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1"
      volumes:
      - name: model-storage
        persistentVolumeClaim:
          claimName: model-storage-pvc
      - name: dataset-volume
        hostPath:
          path: /home/datasets/spotify
          type: Directory
      - name: user-dataset-volume
        hostPath:
          path: /home/lucascarvalho/tp2/dataset
          type: Directory
